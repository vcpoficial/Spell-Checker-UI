{
  "name": "stitch-figma-plugin",
  "version": "1.0.0",
  "scripts": {
    "build": "vite build && esbuild src/code.ts --bundle --outfile=dist/code.js --target=es6",
    "dev": "npm run build -- --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "*",
    "esbuild": "^0.19.0",
    "typescript": "*",
    "vite": "^5.0.0",
    "vite-plugin-singlefile": "^2.0.0"
  }
}
